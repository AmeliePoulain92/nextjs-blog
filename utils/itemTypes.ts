import { NewsletterItemType } from "./enums";

const {
    FeaturedStories,
    PressReleases,
    SponsoredMessage,
    SponsoredPosts
} = NewsletterItemType

export const articleItems = [
    FeaturedStories,
    PressReleases,
    SponsoredMessage,
    SponsoredPosts
]