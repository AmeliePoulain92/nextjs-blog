export enum NewsletterItemAccessType {
    Subscribers = 1,
    NonSubscribers = 2,
    AllUsers = 3
}

export enum NewsletterItemType {
    StandardJobs = 1,
    FeaturedJobs = 2,
    FeaturedStories = 3,
    PressReleases = 4,
    SponsoredPosts = 5,
    Custom = 6,
    LeaderboardAd = 7,
    LeaderboardAdMessage = 8,
    SponsoredMessage = 9
}